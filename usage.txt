usage: mailbot [ -dgev ] [ -l | NAME | -c FILE ]
options:
  -l        list available configs
  NAME      specify config name
  -c FILE   specify config path
  -d        debug trace
  -g        print config and defaults
  -v        print version
  -z        verbose ourput
  -o        offline mode
